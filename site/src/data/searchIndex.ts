// Auto-generated search index — metadata-only
// GUARDRAIL: NO full body text. Fields: name, title, slug, category, tags, description (max 150 chars)

export interface SearchEntry {
  name: string;
  title: string;
  slug: string;
  category: string;
  tags: string[];
  description: string;
}

export const searchIndex: SearchEntry[] = [];
