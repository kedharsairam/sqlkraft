---
title: 21889
topic: "monitor"
description: "Product Name Event ID 21889 Event Source MSSQLSERVER Component SQLEngine Symbolic Name SQLErrorNum21889 Message Text The SQL"
tags: ["monitor","21889"]
pubDate: "2025-12-01"
---

Product

Name

Event ID

21889

Event

Source

MSSQLSERVER

Component

SQLEngine

Symbolic

Name

SQLErrorNum21889

Message

Text

The SQL Server instance '%s' is not a replication publisher. Run

on SQL

Server instance '%s' with distributor '%s' in order to enable the instance to host the

publishing database '%s'. Make certain to specify the same login and password as that

used for the original publisher.

In order to host the publisher database, the instance of SQL Server must be a replication

publisher.

calls

at the remote server to

determine whether the server is a replication publisher. This error indicates that the target

instance of SQL Server is not a replication publisher.

Execute

at the instance of SQL Server that hosts the publisher database.

When running

, specify the correct distributor. Use the same value for the

@password

parameter as that used when

was initially run at the distributor.

ﾉ

Expand table
