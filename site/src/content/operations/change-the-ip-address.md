---
title: "Change the IP Address"
topic: "high-availability"
description: "This topic describes how to change the IP address resource in an Always On Failover Cluster Instance (FCI) by using the Failover Cluster Manager snap-"
tags: ["high-availability","change-the-ip-address"]
pubDate: "2025-12-01"
---

This topic describes how to change the IP address resource in an Always On Failover Cluster

Instance (FCI) by using the Failover Cluster Manager snap-in. The Failover Cluster Manager

snap-in is the cluster management application for the Windows Server Failover Clustering

(WSFC) service.

Security

Before you begin, review the following SQL Server Books Online topic:

Before Installing Failover

Clustering.

To maintain or update an FCI, you must be a local administrator with permission to logon as a

service on all nodes of the FCI.

1. Open the Failover Cluster Manager snap-in.

2. Expand the

node, in the left pane and click on the FCI.

3. On the right pane, under the

category, right-click the SQL Server Instance,

and select

option to open the

dialog box.

4. On the

tab, change the IP address resource.

5. Click

to close the dialog box.

6. In the right-hand pane, right-click the SQL IP Address1(failover cluster instance name)

and select. You will see the SQL IP Address1(failover cluster instance name),
