---
name: "severity-12"
title: "Severity Level 12"
errorNumber: 12
severity: "low"
category: "system"
description: "A special severity for queries that don't use locking because of special query hints. In some cases, read operations performed by these statements could result in inconsistent data because locks aren't taken to guarantee consistency."
tags: ["error-severity","level-12"]
pubDate: 2025-12-01
---

A special severity for queries that don't use locking because of special query hints. In some cases, read operations performed by these statements could result in inconsistent data because locks aren't taken to guarantee consistency.
