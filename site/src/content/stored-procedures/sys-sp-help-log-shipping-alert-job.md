---
name: "sys.sp_help_log_shipping_alert_job"
title: "sp_help_log_shipping_alert_job"
category: "general"
description: "This stored procedure returns the job ID of the alert job from the log shipping monitor. This stored procedure returns the SQL Server Agent job ID of the log shipping alert job. If no log shipping alert job exists, it returns an empty result set. database on the monitor server. fixed server role can run this procedure."
tags: ["stored-procedure"]
pubDate: 2026-05-29
syntax: "sp_help_log_shipping_alert_job"
---

## Description

This stored procedure returns the job ID of the alert job from the log shipping monitor. This stored procedure returns the SQL Server Agent job ID of the log shipping alert job. If no log shipping alert job exists, it returns an empty result set. database on the monitor server. fixed server role can run this procedure.

## Syntax

`sp_help_log_shipping_alert_job`

## Permissions

SQL) 06/23/2025 syntaxsql None. (success) or (failure). This stored procedure returns the SQL Server Agent job ID of the log shipping alert job. If no log shipping alert job exists, it returns an empty result set. must be run from the database on the monitor server. Only members of the fixed server role can run this procedure.
