---
name: 'sys.sp_change_users_login'
title: 'sp_change_users_login'
category: 'general'
description: 'Maps an existing database user to a SQL Server login. Transact-SQL syntax conventions Describes the action for the stored procedure to perform. default, and can have one of the following values. system catalog view in the current database to a SQL Server login of the same name. If a login with the same name doesn''t exist, one is created. Examine the result from the statement, to confirm that the c'
tags: ["stored-procedure"]
pubDate: 2026-05-29
syntax: 'sys.database_principals'
---

## Description

Maps an existing database user to a SQL Server login. Transact-SQL syntax conventions Describes the action for the stored procedure to perform. default, and can have one of the following values. system catalog view in the current database to a SQL Server login of the same name. If a login with the same name doesn't exist, one is created. Examine the result from the statement, to confirm that the correct link is in fact made. Avoid using

## Syntax

```sql
sys.database_principals
```
