---
name: "sys.sp_drop_agent_profile"
title: "sp_drop_agent_profile"
category: "general"
description: "table. This stored procedure is executed at the Transact-SQL syntax conventions The ID of the profile to be dropped. is used in all types of replication. The parameters of the given profile are also dropped from the"
tags: ["stored-procedure"]
pubDate: 2026-05-29
syntax: "sp_drop_agent_profile"
---

## Description

table. This stored procedure is executed at the Transact-SQL syntax conventions The ID of the profile to be dropped. is used in all types of replication. The parameters of the given profile are also dropped from the

## Syntax

```sql
sp_drop_agent_profile
```

## Permissions

06/23/2025 Applies to: SQL Server Drops a profile from the table. This stored procedure is executed at the Distributor on any database. Transact-SQL syntax conventions syntaxsql The ID of the profile to be dropped. @profile_id is , with no default. (success) or (failure). is used in all types of replication. The parameters of the given profile are also dropped from the table. Only members of the fixed server role can execute . Related content Work with Replication Agent Profiles sp_add_agent_profile (Transact-SQL) sp_drop_agent_profile (Transact-SQL) sp_help_agent_parameter (Transact-SQL) Related content
