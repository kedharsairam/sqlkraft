---
name: "sys.dm_db_xtp_nonclustered_index_stats"
title: "sys.dm_db_xtp_nonclustered_index_stats"
category: "in-memory"
description: "system dynamic management view includes statistics about operations on nonclustered indexes in contains one row for each nonclustered index on a memory-optimized table in the current memory index structure is created. In-memory index structures are recreated on database to understand and monitor index activity during DML operations and when a database is brought online. When a database with a memo"
tags: ["in-memory", "dmv"]
pubDate: 2026-05-29
syntax: "sys.dm_db_xtp_nonclustered_index_stats"
---

## Description

system dynamic management view includes statistics about operations on nonclustered indexes in contains one row for each nonclustered index on a memory-optimized table in the current memory index structure is created. In-memory index structures are recreated on database to understand and monitor index activity during DML operations and when a database is brought online. When a database with a memory-

## Syntax

```sql
sys.dm_db_xtp_nonclustered_index_stats
```
