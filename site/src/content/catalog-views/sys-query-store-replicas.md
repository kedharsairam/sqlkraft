---
name: 'sys.query_store_replicas'
title: 'sys.query_store_replicas'
category: 'query-store'
description: 'Average CLR time for the query plan within the'
tags: ["catalog-view", "query-store"]
pubDate: 2026-05-29
---

## Description
Average CLR time for the query plan within the

aggregation interval (reported in microseconds).

Note:

Azure Synapse Analytics will always return

zero (0).

Last CLR time for the query plan within the

aggregation interval (reported in microseconds).

Note:

Azure Synapse Analytics will always return

zero (0).

Minimum CLR time for the query plan within the

aggregation interval (reported in microseconds).

Note:

Azure Synapse Analytics will always return

zero (0).

Maximum CLR time for the query plan within the

aggregation interval (reported in microseconds).

Note:

Azure Synapse Analytics will always return

zero (0).

CLR time standard deviation for the query plan

within the aggregation interval (reported in

microseconds).

Note:

Azure Synapse Analytics will always return

zero (0).

Average DOP (degree of parallelism) for the query

plan within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).

Last DOP (degree of parallelism) for the query plan

within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).

Minimum DOP (degree of parallelism) for the query

plan within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).

Maximum DOP (degree of parallelism) for the query

plan within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).

DOP (degree of parallelism) standard deviation for

the query plan within the aggregation interval.


## Description
Note:

Azure Synapse Analytics will always return

zero (0).

Average memory grant (reported as the number of

8-KB pages) for the query plan within the

aggregation interval. Always 0 for queries using

natively compiled memory optimized procedures.

Note:

Azure Synapse Analytics will always return

zero (0).

Last memory grant (reported as the number of 8-KB

pages) for the query plan within the aggregation

interval. Always 0 for queries using natively

compiled memory optimized procedures.

Note:

Azure Synapse Analytics will always return

zero (0).

Minimum memory grant (reported as the number of

8-KB pages) for the query plan within the

aggregation interval. Always 0 for queries using

natively compiled memory optimized procedures.

Note:

Azure Synapse Analytics will always return

zero (0).

Maximum memory grant (reported as the number of

8-KB pages) for the query plan within the

aggregation interval. Always 0 for queries using

natively compiled memory optimized procedures.

Note:

Azure Synapse Analytics will always return

zero (0).

Memory grant standard deviation (reported as the

number of 8-KB pages) for the query plan within the

aggregation interval. Always 0 for queries using

natively compiled memory optimized procedures.

Note:

Azure Synapse Analytics will always return

zero (0).

Average number of returned rows for the query plan

within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).

Number of returned rows by the last execution of

the query plan within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).


## Description
Minimum number of returned rows for the query

plan within the aggregation interval.

Note:

Azure Synapse Analytics will always return

zero (0).

Maximum number of returned rows for the query

plan within the aggregation interval.

Standard deviation of the number of returned rows

for the query plan within the aggregation interval.

Average number of physical I/O reads for the query

plan within the aggregation interval (expressed as a

number of read I/O operations).

Note:

Azure Synapse Analytics will always return

zero (0).

Last number of physical I/O reads for the query plan

within the aggregation interval (expressed as a

number of read I/O operations).

Note:

Azure Synapse Analytics will always return

zero (0).

Minimum number of physical I/O reads for the

query plan within the aggregation interval

(expressed as a number of read I/O operations).

Note:

Azure Synapse Analytics will always return

zero (0).

Maximum number of physical I/O reads for the

query plan within the aggregation interval

(expressed as a number of read I/O operations).

Note:

Azure Synapse Analytics will always return

zero (0).

Average number of bytes in the database log used

by the query plan, within the aggregation interval.

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Note:

Azure Synapse Analytics will always return

zero (0).

Number of bytes in the database log used by the

last execution of the query plan, within the

aggregation interval.

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.


## Description
Note:

Azure Synapse Analytics will always return

zero (0).

Minimum number of bytes in the database log used

by the query plan, within the aggregation interval.

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Note:

Azure Synapse Analytics will always return

zero (0).

Maximum number of bytes in the database log used

by the query plan, within the aggregation interval.

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Note:

Azure Synapse Analytics will always return

zero (0).

Standard deviation of the number of bytes in the

database log used by a query plan, within the

aggregation interval.

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Note:

Azure Synapse Analytics will always return

zero (0).

Average number of pages used in

for the

query plan within the aggregation interval

(expressed as a number of 8-KB pages).

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Last number of pages used in

for the query

plan within the aggregation interval (expressed as a

number of 8-KB pages).

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Minimum number of pages used in

for the

query plan within the aggregation interval

(expressed as a number of 8-KB pages).

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Maximum number of pages used in

for the

query plan within the aggregation interval

(expressed as a number of 8-KB pages).


## Description
Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Number of pages used in

standard deviation

for the query plan within the aggregation interval

(expressed as a number of 8-KB pages).

Applies to:

SQL Server (Starting with SQL Server

2017 (14.x)) and Azure SQL Database.

Average number of page server I/O reads for the

query plan within the aggregation interval

(expressed as a number of 8-KB pages read).

Applies to:

Azure SQL Database Hyperscale

Note:

Azure Synapse Analytics, Azure SQL Database,

Azure SQL Managed Instance (non-hyperscale) will

always return zero (0).

Last number of page server I/O reads for the query

plan within the aggregation interval (expressed as a

number of 8-KB pages read).

Applies to:

Azure SQL Database Hyperscale

Note:

Azure Synapse Analytics, Azure SQL Database,

Azure SQL Managed Instance (non-hyperscale) will

always return zero (0).

Minimum number of page server I/O reads for the

query plan within the aggregation interval

(expressed as a number of 8-KB pages read).

Applies to:

Azure SQL Database Hyperscale

Note:

Azure Synapse Analytics, Azure SQL Database,

Azure SQL Managed Instance (non-hyperscale) will

always return zero (0).

Maximum number of page server I/O reads for the

query plan within the aggregation interval

(expressed as a number of 8-KB pages read).

Applies to:

Azure SQL Database Hyperscale

Note:

Azure Synapse Analytics, Azure SQL Database,

Azure SQL Managed Instance (non-hyperscale) will

always return zero (0).

Standard deviation of the number of page server I/O

reads for the query plan within the aggregation


## Description
interval (expressed as a number of 8-KB pages read).

Applies to:

Azure SQL Database Hyperscale

Note:

Azure Synapse Analytics, Azure SQL Database,

Azure SQL Managed Instance (non-hyperscale) will

always return zero (0).

Identifies the replica set number for this replica.

Foreign key to

sys.query_store_replicas

.

Applies to:

SQL Server (Starting with SQL Server

2022 (16.x))

Requires the

permission.

In rare situations, you might notice the DOP columns (max_dop, min_dop, avg_dop and

last_dop) report large numbers. This might happen when queries execute on systems with large

number of processors. You might notice this discrepancy in scenarios where the query uses

user defined functions. The engineering team is investigating this issue and will update this

note when this issue is fixed. This is a reporting issue in the statistics gathered for this catalog

view and does not impact the actual query execution or its runtime performance.

Learn more about Query Store in the following articles:

sys.query_store_replicas (Transact-SQL)

sys.database_query_store_options (Transact-SQL)

sys.query_context_settings (Transact-SQL)

sys.query_store_plan (Transact-SQL)

sys.query_store_query (Transact-SQL)

sys.query_store_query_text (Transact-SQL)

sys.query_store_wait_stats (Transact-SQL)

sys.query_store_runtime_stats_interval (Transact-SQL)

Monitoring Performance By Using the Query Store

Catalog Views (Transact-SQL)

Next steps

Query Store Stored Procedures (Transact-SQL)


## Best Practice with the Query Store
Last updated on 11/18/2025

```sql
tempdb
```

```sql
tempdb
```

```sql
tempdb
```

```sql
tempdb
```

```sql
tempdb
```

```sql
VIEW DATABASE STATE
```
