---
name: "sys.availability_groups"
title: "sys.availability_groups"
category: "compatibility"
description: "Returns a row for each availability group where the local instance of SQL Server hosts an availability replica. Each row contains a cached copy of the availability group metadata. Unique identifier (GUID) of the Name of the availability group."
tags: ["compatibility", "catalog-view"]
pubDate: 2026-05-29
syntax: "failure_condition_level"
---

## Description

Returns a row for each availability group where the local instance of SQL Server hosts an availability replica. Each row contains a cached copy of the availability group metadata. Unique identifier (GUID) of the Name of the availability group.

## Syntax

`failure_condition_level`
