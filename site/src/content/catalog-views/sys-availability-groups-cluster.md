---
name: "sys.availability_groups_cluster"
title: "sys.availability_groups_cluster"
category: "compatibility"
description: "Returns a row for each Always On availability group in Windows Server Failover Clustering (WSFC). Each row contains the availability group metadata from the WSFC cluster. Unique identifier (GUID) of the availability group."
tags: ["compatibility","catalog-view"]
pubDate: 2026-05-29
syntax: "failure_condition_level"
---

## Description

Returns a row for each Always On availability group in Windows Server Failover Clustering (WSFC). Each row contains the availability group metadata from the WSFC cluster. Unique identifier (GUID) of the availability group.

## Syntax

`failure_condition_level`
