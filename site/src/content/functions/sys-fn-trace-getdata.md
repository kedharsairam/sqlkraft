---
name: 'sys.fn_trace_getdata'
title: 'snapshots.fn_trace_getdata'
category: 'system'
description: 'This function returns all the events captured for the specified trace. Transact-SQL syntax conventions The unique identifier for the primary key in the snapshots.trace_info table in the management The time that the trace started. The time that the trace ended. The trace data from the snapshots.trace_data table in the management data A list of the columns for the specified trace can be obtained by '
tags: ["system", "function"]
pubDate: 2026-05-29
syntax: 'SELECT * FROM sys.trace_columns'
---

## Description

This function returns all the events captured for the specified trace. Transact-SQL syntax conventions The unique identifier for the primary key in the snapshots.trace_info table in the management The time that the trace started. The time that the trace ended. The trace data from the snapshots.trace_data table in the management data A list of the columns for the specified trace can be obtained by using the

## Syntax

```sql
SELECT * FROM sys.trace_columns
```
